meta {
  name: Sletter et rum fra systemet.
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/Rooms/:id
  body: none
  auth: none
}

params:path {
  id: {{room-id}}
}

headers {
  Authorization: Bearer {{JWT-Admin}}
}

tests {
  // Test status kode
  test("Status kode skal være 204 OK", function() {
    expect(res.status).to.equal(204);
  });
}
