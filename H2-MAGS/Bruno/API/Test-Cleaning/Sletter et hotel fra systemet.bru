meta {
  name: Sletter et hotel fra systemet.
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/Hotels/:id
  body: none
  auth: none
}

params:path {
  id: {{hotel-id}}
}

headers {
  Authorization: {{apiKey}}
}

tests {
  // Test status kode
  test("Status kode skal være 204 OK", function() {
    expect(res.status).to.equal(204);
  });
}
