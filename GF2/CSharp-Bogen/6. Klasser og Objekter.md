### Klasser og Objekter

Klasser og objekter er fundamentale begreber i objektorienteret programmering (OOP) med C#. Når vi udvikler med C# og følger OOP-principperne, opretter vi klasser, som fungerer som skabeloner til at oprette objekter.

En klasse definerer de egenskaber og adfærd, som objekter af den pågældende klasse kan have. Egenskaber repræsenterer tilstanden eller dataene, der er knyttet til et objekt, og adfærd repræsenterer de handlinger eller metoder, som objektet kan udføre.

Lad os tage et eksempel på en klasse kaldet "Car" (bil):

```csharp
public class Car
{
    // Egenskaber
    public string Brand;
    public string Model;
    public int Year;

    // Metoder
    public void StartEngine()
    {
        Console.WriteLine("Engine started.");
    }

    public void Accelerate()
    {
        Console.WriteLine("Car is accelerating.");
    }
}

```

I dette eksempel definerer klassen "Car" tre egenskaber: Brand, Model og Year, som repræsenterer bilens mærke, model og årgang. Klassen har også to metoder: StartEngine (StartMotor) og Accelerate (Accelerer), som repræsenterer handlingerne at starte motoren og accelerere bilen.

Når vi ønsker at oprette objekter af klassen "Car", bruger vi "new" nøgleordet:

```csharp
Car myCar = new Car();

```

Dette opretter et nyt objekt af typen "Car" og tildeler det til variablen "myCar". Nu kan vi arbejde med objektet ved at tilgå dets egenskaber og kalde dets metoder. For eksempel kan vi indstille bilens mærke og model:

```csharp
myCar.Brand = "Ford";
myCar.Model = "Mustang";
```

Alternativt kan man skrive det ind med Objekt notation med det samme

```csharp
Car myCar = new Car()
{
    Brand = "Ford",
    Model = "Mustang",
    Year = 1961
};
```

![Untitled](https://prod-files-secure.s3.us-west-2.amazonaws.com/78111fd7-5d55-4196-af1c-1918b8dd24a0/e6115e2b-3550-403c-a691-208ca0049e85/Untitled.png)

Vi kan også kalde objektets metoder:

```csharp
myCar.StartEngine();
myCar.Accelerate();

```

Når vi opretter objekter af en klasse, kan hvert objekt have forskellige værdier for egenskaberne, men de deler de samme metoder og deres adfærd.

### Properties med get og set

I stedet for at bruge offentlige felter (`public string Brand;`) bruger man ofte **properties** med `get` og `set`. Det giver mulighed for at validere værdier eller beregne noget ved læsning.

```csharp
public class BankKonto
{
    public string Kontonummer { get; set; }
    private double _saldo;  // privat felt
    public double Saldo
    {
        get => _saldo;
        set { if (value >= 0) _saldo = value; }  // kun ikke-negative værdier
    }
}
```

En **read-only property** (kun læsning) kan beregnes ud fra andre felter, f.eks. `public double Areal => Laengde * Bredde;`

### Constructors (konstruktører)

En **constructor** er en speciel metode der kaldes når objektet oprettes med `new`. Den har samme navn som klassen og ingen returtype. Du kan tvinge at visse værdier skal angives.

```csharp
public class Hund
{
    public string Navn { get; set; }
    public string Race { get; set; }
    public Hund(string navn, string race)
    {
        Navn = navn;
        Race = race;
    }
}
Hund h = new Hund("Bella", "Labrador");
```

Du kan have **flere constructors** (overload): fx én med alle parametre og én **default constructor** uden parametre (`public Bog() { }`), så man kan sætte properties bagefter.

### Arv (Inheritance)

En klasse kan **arve** fra en anden med `: BaseKlasse`. Den afledte klasse får alle egenskaber og metoder fra base-klassen og kan tilføje egne.

```csharp
public class Dyr
{
    public string Navn { get; set; }
    public int Alder { get; set; }
}
public class Hund : Dyr
{
    public string Race { get; set; }  // kun på Hund
}
Hund h = new Hund { Navn = "Bella", Alder = 5, Race = "Labrador" };
```

Et andet eksempel kan være på at vi modellere dyr som klasser og objekter i C#

Klasser er en skabelon for at lave objekter - vi kan altså bruge klasser til at lave flere objekter.

Som vi kan se indeholder en klasse nogle standart egenskaber (Properties) og Methods (Metoder også kaldet funktioner)

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/24ba1fe9-5f17-4b6b-af5a-6f8db2cfd1de/Untitled.png)

```csharp
// Kode for billede
public class Dog
{
    public string color;
    public string eyeColor;
    public string height;
    public string length;
    public string weight;
		public int numberOfLegs;

    public void Sit()
    {
        Console.WriteLine("Dog sits");
    }

    public void LayDown()
    {
        Console.WriteLine("Dog lays down");
    }

    public void Shake()
    {
        Console.WriteLine("Dog shakes");
    }

    public void Comes()
    {
        Console.WriteLine("Dog came to you");
    }
}

Dog Bobby = new Dog("Yellow", "Brown", "17 in", "35 in", "24 Pounds");
Bobby.height // 17 in
Bobby.Sit(); // Dog Sits

```

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/de2bcbc3-eb2d-4fc3-8883-847348c2976c/Untitled.png)

---
## Mere information

**Videoer (Bro Code)**  
![C# classes](https://www.youtube.com/watch?v=9V5B3dNoVIA)  
![C# objects](https://www.youtube.com/watch?v=heoTab1e41A)  
![C# constructors](https://www.youtube.com/watch?v=cAhh6pYkHPQ)  
![C# static](https://www.youtube.com/watch?v=8xcIy9cV-6g)  
![C# getters & setters](https://www.youtube.com/watch?v=8FmE_-QXg3Y)  
![C# enums](https://www.youtube.com/watch?v=3p0OJErAbEI)

**C# for Beginners (Microsoft)**  
![Object-oriented Programming (OOP) [Pt 18]](https://www.youtube.com/watch?v=Vp0vVzJgJ5g)  
[OOP with derived or abstract classes, overrides [Pt 19]](https://learn.microsoft.com/en-us/shows/csharp-for-beginners/oop-with-derived-or-abstract-classes-overrides-ienumerable-csharp-for-beginners/)  
*[Hele serien (19 afsnit)](https://www.youtube.com/playlist?list=PLdo4fOcmZ0oULFjxrOagaERVAMbmG20Xe)*

*Tilføj gerne flere videoer og ressourcer her.*