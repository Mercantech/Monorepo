meta {
  name: Sletter en booking fra systemet.
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/Bookings/:id
  body: none
  auth: none
}

params:path {
  id: {{booking-id}}
}

headers {
  Authorization: Bearer {{JWT-Admin}}
}

tests {
  // Test status kode
  test("Status kode skal være 204 OK", function() {
    expect(res.status).to.equal(204);
  });
}
