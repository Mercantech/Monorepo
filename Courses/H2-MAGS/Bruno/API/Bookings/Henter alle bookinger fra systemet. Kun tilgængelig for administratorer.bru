meta {
  name: Henter alle bookinger fra systemet. Kun tilgængelig for administratorer.
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/Bookings
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{JWT-Admin}}
}

tests {
  // Test status kode
  test("Status kode skal være 200 No Content", function() {
    expect(res.status).to.equal(200);
  });
}
