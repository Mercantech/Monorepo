meta {
  name: 5-StartSession
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/QuizSession/{{sessionId}}/start
  body: none
  auth: none
}

tests {
  test("Status er 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
